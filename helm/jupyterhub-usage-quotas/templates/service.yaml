apiVersion: v1
kind: Service
metadata:
  name: {{ include "jupyterhub-usage-quotas.server.fullname" . }}
  labels: {{- include "jupyterhub-usage-quotas.labels" . | nindent 4 }}
  {{- with .Values.service.annotations }}
  annotations:
    {{- . | toYaml | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      {{- with .Values.service.nodePort }}
      nodePort: {{ . }}
      {{- end }}
  selector: {{- include "jupyterhub-usage-quotas.selectorLabels" . | nindent 4 }}
